{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install scikit-learn numpy requests python-dotenv joblib fastapi",
  "buildCommand": "echo deploy-ready",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
